---
interface Props {
  class?: string;
  title: string;
  description: string;
  authorName: string;
  url?: string;
}

const { class: className, title, description, authorName, url } = Astro.props;
---

<div class:list={['example-card', className]}>
  <h2 class="example-card-title">
    {title}
  </h2>

  <p class="example-card-description">
    {description}
  </p>

  <div class="example-card-author">
    {authorName}
  </div>

  <a href={url} class="example-card-button"> Читать статью → </a>
</div>

<style lang="scss">
  @use '@styles/base' as *;

  .example-card {
    @include flex-column;

    max-width: 380px;
    width: 100%;
    background: white;
    border-radius: 24px;
    padding: 28px;
    box-shadow: 0 20px 40px -12px rgb(0 0 0 / 10%);
    font-family: Inter, sans-serif;
    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease;
    border: 1px solid rgb(0 0 0 / 5%);
  }

  .example-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 30px 50px -12px rgb(0 0 0 / 15%);
  }

  .example-card-title {
    font-size: 24px;
    font-weight: 700;
    color: #1a1a2c;
    margin-bottom: 12px;
    line-height: 1.3;
    letter-spacing: -0.02em;
  }

  .example-card-description {
    font-size: 15px;
    line-height: 1.5;
    color: #4a4a68;
    margin-bottom: 20px;
    font-weight: 400;
  }

  .example-card-author {
    margin-top: auto;

    font-size: 14px;
    color: #667eea;
    font-weight: 500;
    margin-bottom: 24px;
    padding-bottom: 20px;
    border-bottom: 1px solid rgb(0 0 0 / 5%);
  }

  .example-card-button {
    background: #667eea;
    color: white;
    border: none;
    padding: 12px 24px;
    border-radius: 40px;
    font-size: 15px;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s ease;
    width: 100%;
    text-decoration: none;
    display: inline-block;
    text-align: center;
  }

  .example-card-button:hover {
    background: #5a67d8;
    transform: scale(1.02);
  }

  @media (max-width: 480px) {
    .example-card {
      padding: 24px;
    }

    .example-card-title {
      font-size: 22px;
    }
  }

  @media (prefers-color-scheme: dark) {
    .example-card {
      background: #1e1e2e;
      border-color: rgb(255 255 255 / 5%);
    }

    .example-card-title {
      color: #fff;
    }

    .example-card-description {
      color: #b8b8d0;
    }

    .example-card-author {
      color: #9f7aea;
      border-bottom-color: rgb(255 255 255 / 5%);
    }
  }
</style>
